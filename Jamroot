path-constant TOP : . ;
include $(TOP)/sanity.jam ;

boost 103600 ;
project : requirements $(requirements) ;
project : default-build <threading>multi <warnings>on <variant>release ;

external-lib z ;

build-project lm ;
build-project util ;

install-bin-libs lm//query lm//build_binary lm//kenlm_max_order ;
install-headers headers : [ glob-tree *.hh : dist include ] : . ;
alias install : prefix-bin prefix-lib prefix-include ; 
explicit headers ;
explicit install ;
